<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    
    <div class=" p-10 w-full min-h-screen bg-zinc-900 text-white">
        
        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md text-sm inline-block mb-5 px-3 py-2" href="/logout">Logout</a>
        </div>
       <div class="flex gap-3 items-start overflow-hidden" >
         <div class = "w-10 h-10 bg-red-500 rounded-md">
                <img class="w-full h-full object-cover" src = "/images/uploads/<%= user.profilePic %>" alt= "sv"/>
            </div>
            <h3 class="text-3xl "> <span class="font-light">Hello</span> <%= user.name %> 😊 </h3>
        </div>
        
       
        <h5 class="mb-5 mt-3">You can create a post. </h5>
        <form  method="post" action="/post">
            <textarea name="content" class=" mb-3 block text-white w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md p-3 outline-none" placeholder="Whats on your mind ? "></textarea>
            <input type="submit" class="w-40 block px-3 py-2 text-sm  bg-blue-500 rounded-md" value="Create new Post"/>
        </form>
        <div class="posts mt-32">
            <h3 class="text-zinc-400">Your Posts</h3>
            </div>
            <div class="post-container mt-5">
                <% user.posts.reverse().forEach(function(post){ %>
                        <div class="post w-1/3 mb-3 p-4 mb-4 rounded-md border-[1px] border-zinc-700 bg-zinc-800">
                            <h4 class="text-blue-500 mb-2">
                                @<%= user.username %>
                            </h4>
                            <p class= "text-sm tracking-tight">
                                <%= post.content %>
                            </p> 
                            <small class="mt-2 inline-block" > <%= post.likes.length %> Likes</small>

                            <div class="btns flex gap-4 mt-1">
                                <a class="text-blue-500 " href="/like/<%= post._id%>"><%= post.likes.indexOf(user._id) === -1  ? "Like" : "UnLike"%></a>
                                <a class="text-zinc-400 " href="/edit/<%= post._id%>">Edit</a>
                            </div>

                        </div>
               <% }) %>
                </div>
            </div>
        

    </div>

</body>
</html>